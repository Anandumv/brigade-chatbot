# Web Framework
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6

# Database (Pixeltable without video/av dependency)
# Core pixeltable deps (manually specified to skip av)
psycopg2-binary>=2.9.9
sqlalchemy>=2.0.0
pgvector>=0.2.1
cloudpickle>=2.2.1
tqdm>=4.64
pillow>=9.3.0
jinja2>=3.1.3
puremagic>=1.20
lxml>=5.0
more-itertools>=10.2
toml>=0.10
pyarrow>=13.0.0,<18.0.0  # Pin to avoid source build
jsonschema>=4.0.0
ftfy>=6.0.0
beautifulsoup4>=4.12.0
pymupdf>=1.23.0  # Required for fitz in pixeltable
jmespath>=1.0.0
# lxml>=4.9.0  # Required for bs4 lxml parser in pixeltable (Already covered by line 16)
# Pixeltable itself (installed with --no-deps in build command)
# Pixeltable dependencies
opencv-python-headless>=4.8.0
scipy>=1.10.0
av>=12.0.0

# OpenAI
openai>=1.40.0,<2.0.0
tiktoken>=0.5.0,<0.8.0  # Pin to pre-built wheel version

# Web Search
tavily-python>=0.3.3

# Document Processing
PyPDF2>=3.0.1
pdfplumber>=0.10.4,<0.11.0
openpyxl>=3.1.2
pandas>=2.2.0

# Vector Operations
numpy>=1.26.3

# Environment Variables
python-dotenv>=1.0.0

# HTTP Client
httpx>=0.27.0  # Upgraded to fix httpcore conflict with Pixeltable

# Data Validation
pydantic>=2.8.0  # Upgraded for Pixeltable compatibility
pydantic-settings>=2.3.0

# Utilities
nest_asyncio>=1.6.0
tenacity==8.2.3  # Retry logic
python-json-logger==2.0.7

# Testing
pytest==7.4.4
pytest-asyncio==0.23.3
pytest-cov==4.1.0

# Type Checking
mypy==1.8.0

# Code Quality
black==24.1.0
ruff==0.1.13

# Redis (for session persistence)
redis>=5.0.0
hiredis>=2.3.0
